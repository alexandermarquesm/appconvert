[pytest]
markers =
    config: mark a config as a configuration.
    api: api a api as a api_restful.
    pages: pages a as a pages of website.
    resources: resources a as a resouce of api_restful

addopts = -p no:warnings